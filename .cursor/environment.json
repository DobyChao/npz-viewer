{
  "name": "npz-viewer",
  "install": "bash .cursor/install.sh",
  "terminals": [
    {
      "name": "backend",
      "command": "cd backend && ../.venv/bin/python -m app.main --host 127.0.0.1 --port 8756",
      "description": "FastAPI backend (npz scan/decode/render API) on http://127.0.0.1:8756"
    },
    {
      "name": "frontend",
      "command": "cd frontend && npm run dev",
      "description": "Vite dev server on http://127.0.0.1:5273; proxies /api to the backend"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 5273 },
    { "name": "backend", "port": 8756 }
  ]
}
